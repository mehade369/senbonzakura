import 'package:flutter/material.dart';

//  Long the future should await only used in development mode.
const futureDelayed = Duration(microseconds: 800);

const kAnimationDuration = Duration(milliseconds: 500);

const kRadius = 30.0;

const kPadding = 10.0;
const kPaddingAll = EdgeInsets.all(kPadding);
const kPaddingTop = EdgeInsets.only(top: kPadding);
const kPaddingBottom = EdgeInsets.only(bottom: kPadding);
const kPaddingLeft = EdgeInsets.only(left: kPadding);
const kPaddingRight = EdgeInsets.only(right: kPadding);
const kPaddingVertical = EdgeInsets.symmetric(vertical: kPadding);
const kPaddingHorizontal = EdgeInsets.symmetric(horizontal: kPadding);
